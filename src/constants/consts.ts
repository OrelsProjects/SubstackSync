export const MAX_FILE_SIZE = 5 * 1024 * 1024; // 10MB
export const CHUNK_SIZE = 3 * 1024 * 1024; // 3MB
export const MAX_ATTACHMENTS = 5;
export const MAX_FILES = 3;
export const MAX_FILE_PAGES = 30;
export const MAX_FILE_SIZE_STRING = `${MAX_FILE_SIZE / (1024 * 1024)}MB`;
export const ALLOWED_FILE_TYPES = [".pdf"];
export const ALLOWED_MIME_TYPES = ["application/pdf"];
